version: '3.8'
services:
  mongo:
    image: mongo:7.0
    container_name: ai-companion-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: ai_companion
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data:
